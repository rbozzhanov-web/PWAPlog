// Form-level validation will be ported with the web entry editor; the shared domain module is
// intentionally dependency-free so it can run in browser, Node, and native-adjacent contexts.
export {};
