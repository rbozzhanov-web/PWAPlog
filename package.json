{
  "name": "pilot-logbook",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @pilot-logbook/web run dev",
    "build": "npm --workspace @pilot-logbook/web run build",
    "test": "vitest run --workspace vitest.workspace.ts",
    "test:web": "vitest run --project web",
    "deploy:cloudflare": "npm run build && npx wrangler@latest pages deploy",
    "generate:norm-airports": "node scripts/generate-norm-airports.mjs",
    "generate:icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.20.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vite-plugin-pwa": "^0.21.1",
    "vitest": "^3.2.4"
  }
}
